# Examples

这个文件夹包含了各种尾递归优化的实际使用示例。

## 运行示例

首先确保已经构建了插件：

```bash
cd ..
npm install
npm run build
```

然后使用 Babel 运行示例：

```bash
# 使用 babel-node (需要安装 @babel/node)
npx babel-node --plugins ../dist/index.js fibonacci.js

# 或者先编译再运行
npx babel --plugins ../dist/index.js fibonacci.js -o fibonacci.compiled.js
node fibonacci.compiled.js
```

## 示例列表

### 📊 [fibonacci.js](./fibonacci.js)
斐波那契数列计算
- 展示经典尾递归优化
- 使用 BigInt 支持大数计算
- 性能对比：优化 vs 未优化

### 📝 [array-operations.js](./array-operations.js)
数组操作集合
- sum - 数组求和
- filter - 过滤元素
- map - 映射转换
- reduce - 归约操作
- find - 查找元素
- flatten - 数组扁平化

### 🔤 [string-operations.js](./string-operations.js)
字符串操作集合
- reverse - 字符串反转
- isPalindrome - 回文检查
- countChar - 字符计数
- repeat - 字符串重复
- removeSpaces - 删除空格
- capitalize - 首字母大写

## 学习要点

### 1. 尾递归模式识别

✅ **尾递归（会被优化）:**
```javascript
function sum(n, acc = 0) {
  if (n === 0) return acc;
  return sum(n - 1, acc + n);  // ← 递归调用是最后一个操作
}
```

❌ **非尾递归（不会被优化）:**
```javascript
function sum(n) {
  if (n === 0) return 0;
  return n + sum(n - 1);  // ← 递归调用后还有 + 操作
}
```

### 2. 累加器模式

尾递归通常需要累加器参数来保存中间结果：

```javascript
// 转换前（非尾递归）
function factorial(n) {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}

// 转换后（尾递归）
function factorial(n, acc = 1) {
  if (n <= 1) return acc;
  return factorial(n - 1, n * acc);
}
```

### 3. 性能优势

- ✅ 防止栈溢出
- ✅ O(1) 空间复杂度（循环等价）
- ✅ 接近手写循环的性能
- ✅ 保持函数式编程风格

## 常见问题

**Q: 为什么我的递归函数没有被优化？**

A: 确保满足以下条件：
1. 递归调用在尾位置（最后一个操作）
2. 没有在递归调用后进行其他计算
3. 函数名在作用域内可见（不是匿名函数）

**Q: 箭头函数支持吗？**

A: 支持！只要满足尾递归条件：
```javascript
const sum = (n, acc = 0) => 
  n === 0 ? acc : sum(n - 1, acc + n);
```

**Q: 条件表达式中的递归会被优化吗？**

A: 会！支持三元运算符和逻辑表达式：
```javascript
function f(n) {
  return n === 0 ? 0 : f(n - 1);  // ✅ 支持
}
```

## 更多资源

- [主要 README](../README.md)
- [贡献指南](../CONTRIBUTING.md)
- [测试用例](../test/)
- [性能基准](../benchmark/)
